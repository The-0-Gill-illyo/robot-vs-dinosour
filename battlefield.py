class BattleField:

    def __init__(self, winnner, start, stage):
        self.run_game = start
        self.display_message = "words"
        self.battle_phase = stage
        self.display_winner = winnner
